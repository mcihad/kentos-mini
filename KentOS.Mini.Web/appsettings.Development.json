{
  // Geliştirme koşumu. Kuruma özel bilgi ve sır BURAYA DA YAZILMAZ —
  // hepsi çözüm kökündeki `.env` dosyasından gelir.
  //
  // Postgres, /Users/cihad/Projects/database içindeki paylaşılan `postgis_db`
  // konteynerinde çalışır — aynı konteynerde başka projelerin veritabanları da
  // var, onlara dokunulmaz.

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
